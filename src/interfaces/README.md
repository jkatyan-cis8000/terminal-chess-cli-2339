# Providers Layer

This layer contains cross-cutting concerns: auth, telemetry, connectors, flags.

For this chess game, providers may include:
- Input provider (reading from stdin)
- Output provider (writing to stdout)
- Random provider (if needed for AI in future)
